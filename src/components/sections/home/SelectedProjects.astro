---
// components
import Container from "../../elements/Container.astro";
import Heading from "../../elements/Heading.astro";
import Work from "../../elements/Work.astro";
import Link from "../../elements/Link.astro";

// content collections
import { getCollection } from "astro:content";
const works = await getCollection("works");
const featuredWorks = works.filter(work => work.data.isFeatured === true).sort((a, b) => a.data.order - b.data.order);
---

<Container variant={'lg'} class="items-center">
    <Heading tagName="h2" tagSize="h2" class="text-center">
        Featured Projects
    </Heading>
    <Container class="grid lg:grid-cols-2 grid-cols-1 gap-16 justify-items-center">
        {featuredWorks.map((work) => (
            <Work 
                slug={work.data.slug}
                featuredImage={work.data.featuredImage}
                title={work.data.title}
                platforms={work.data.platforms}
                category={work.data.category}>
            </Work>
        ))
        }
    </Container>
    <Link variant={'button'} href="/projects">All Projects</Link>
</Container>